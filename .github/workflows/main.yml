on:
  workflow_dispatch
name: 🚀 Deploy server (Gamemode) 🚀
jobs:
  gamemode-deploy:
    name: 🎉 Deploy to live  (Gamemode) 🎉
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code (Gamemode) 
      uses: actions/checkout@v2
    
    - name: 📂 Sync Gamemode
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: 216.52.25.77
        port: 8822
        username: nathan6
        password: ${{ secrets.ftp_password }}
        server-dir: /216.52.25.77_27055/garrysmod/gamemodes/
        exclude: .git*
          - **.git*
          - .git*/**
          -  **/.git*/**
          - assets/**
          - .glualint.json
          - addons/**
  

on:
  workflow_dispatch
name: 🚀 Deploy server (Addon) 🚀
jobs:
  gamemode-deploy:
    name: 🎉 Deploy to live  (Addon) 🎉
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code  (Addon) 
      uses: actions/checkout@v2
    
    - name: 📂 Sync Addons
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: 216.52.25.77
        port: 8822
        username: nathan6
        password: ${{ secrets.ftp_password }}
        server-dir: /216.52.25.77_27055/garrysmod/
        exclude: .git*
          - **.git*
          - .git*/**
          -  **/.git*/**
          - assets/**
          - .glualint.json
          - nutscript/**
          - stalker/**
